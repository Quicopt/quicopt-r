# A named index set with concrete elements

A named index set with concrete elements

## Usage

``` r
index_set(name, elements)
```

## Arguments

- name:

  The set's name.

- elements:

  A list of integers and strings.
