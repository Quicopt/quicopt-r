# Maintainer-local context — never tracked (family convention)
/CLAUDE.md
/.claude/
/SPEC.md

.Rproj.user/
.Rhistory
.RData
.DS_Store
*.tar.gz
quicopt.Rcheck/

# pkgdown builds the site here; CI publishes it to gh-pages
/docs/
